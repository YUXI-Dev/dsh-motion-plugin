{
  "name": "dsh-motion-complete",
  "version": "1.1.0",
  "description": "DeepSeek Harness 完整动效主题包：把 DSH 0.1.0-rc.6 / 0.1.0-rc.7 安装中全部已定稿的 UI 动效改动固化为即装即用、可卸载还原、版本可校验的补丁包",
  "type": "module",
  "bin": {
    "dsh-motion-complete": "bin/apply.mjs"
  },
  "files": [
    "bin",
    "patches",
    "README.md",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "scripts": {
    "apply": "node bin/apply.mjs",
    "revert": "node bin/apply.mjs --revert",
    "check": "node bin/apply.mjs --check"
  }
}
